# kind 集群配置 — 支持 NodePort
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: hermes-runtime
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 30080
        protocol: TCP
